sudo: required

language: C

services:
      - docker

before_install:
    - docker info
    - docker build -t ntonjeta/iidea-Docker .

after_success:
    - ./opt/llvm/install-clang

#script:
#    - insert example script to run
